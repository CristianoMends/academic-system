spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/academic_system_db
    username: postgres
    password: cristiano1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true


